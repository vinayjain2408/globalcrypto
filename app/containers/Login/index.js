import { LoginNav, LoginPage } from '@/components';
import React from 'react';

function index() {
  return (
    <div>
      <LoginNav />
      <LoginPage />
    </div>
  );
}

export default index;
